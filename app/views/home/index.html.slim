.landing(ng-controller="LandingController")
  =video_tag("ParliamentHill.mov", autoplay: true, loop: true)
  h1.logo Guvanuh
  form.search-bar
    input.search(ng-model="query" placeholder="Search for an act or regulation here")
    button(type="button")
      i.fa.fa-search